import { Users, Home } from "lucide-react";

function Navbar() {
  return (
    <nav style={{
      display: "flex",
      gap: "20px",
      padding: "15px",
      background: "#0f172a",
      color: "white"
    }}>
      <Home size={24} /> Home
      <Users size={24} /> Clients
    </nav>
  );
}

export default Navbar;
